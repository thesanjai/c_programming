//Factorial without using function
#include<stdio.h>
#include<stdlib.h>
#include<string.h>

int main(){
    
    char* str;
    printf("Enter a string: ");
   // gets(str);
   fgets(str,6,stdin);

    puts(str);

    return 0;
}